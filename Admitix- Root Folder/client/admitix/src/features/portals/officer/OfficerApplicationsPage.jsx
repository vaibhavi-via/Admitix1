
import {useEffect,useState}from'react';import{useNavigate}from'react-router-dom';import{RefreshCw,Search}from'lucide-react';import{getApplications}from'./api';import{Card,Badge,Loading,ErrorBox}from'../components/PortalUI'
export default function OfficerApplicationsPage(){const[apps,setApps]=useState([]);const[q,setQ]=useState('');const[loading,setLoading]=useState(true);const[error,setError]=useState('');const nav=useNavigate();const load=()=>{setLoading(true);getApplications().then(setApps).catch(e=>setError(e.message)).finally(()=>setLoading(false))};useEffect(load,[]);if(loading)return <Loading/>;const rows=apps.filter(a=>a.application_number?.toLowerCase().includes(q.toLowerCase())||a.current_status?.includes(q.toLowerCase()));return <div className="space-y-5"><div className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><h2 className="text-xl font-bold">Assigned Applications</h2><p className="mt-1 text-sm text-slate-500">Applications currently assigned to you.</p></div><button onClick={load} className="inline-flex items-center gap-2 self-start rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm font-medium"><RefreshCw size={15}/> Refresh</button></div><ErrorBox message={error}/><Card><div className="relative"><Search className="absolute left-3 top-3 text-slate-400" size={16}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search application number or status..." className="w-full rounded-lg border border-slate-200 py-2.5 pl-9 pr-3 text-sm"/></div><div className="mt-4 overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr className="border-b border-slate-100 text-xs text-slate-400"><th className="px-3 py-3">Application</th><th className="px-3 py-3">Status</th><th className="px-3 py-3">Submitted</th><th className="px-3 py-3"></th></tr></thead><tbody>{rows.map(a=><tr key={a.application_id} className="border-b border-slate-50"><td className="px-3 py-3 font-medium">{a.application_number}</td><td className="px-3 py-3"><Badge status={a.current_status}/></td><td className="px-3 py-3 text-slate-500">{new Date(a.submission_date).toLocaleDateString()}</td><td className="px-3 py-3 text-right"><button onClick={()=>nav(`/officer/applications/${a.application_id}`)} className="font-semibold text-emerald-600">Review</button></td></tr>)}</tbody></table>{!rows.length&&<p className="py-8 text-center text-sm text-slate-400">No assigned applications found.</p>}</div></Card></div>}
