import{useState}from'react';import PageHeader from'../../../components/crud/PageHeader';import{verifyAndPersistDocument}from'../services';export default function AiVerificationCreatePage(){const[id,setId]=useState('');const[file,setFile]=useState(null);const[r,setR]=useState(null);const[e,setE]=useState('');const[b,setB]=useState(false);const submit=async x=>{x.preventDefault();if(!id||!file)return setE('Document ID and file are required.');setB(true);setE('');try{setR(await verifyAndPersistDocument(id,file))}catch(x){setE(x.message||'AI verification failed.')}finally{setB(false)}};return <div><PageHeader title="AI Document Verification" subtitle="Run AI analysis and persist the result."/><div className="grid gap-6 lg:grid-cols-3"><form onSubmit={submit} className="space-y-5 rounded-2xl border bg-white p-6 shadow-sm"><div><label>Document ID</label><input value={id} onChange={e=>setId(e.target.value)} placeholder="Existing document UUID"/></div><div><label>Document file</label><input type="file" accept=".pdf,.jpg,.jpeg,.png,.webp" onChange={e=>setFile(e.target.files?.[0]||null)} className="block w-full rounded-xl border p-3"/></div>{e&&<p className="rounded-xl bg-red-50 p-3 text-sm text-red-600">{e}</p>}<button disabled={b} className="w-full rounded-xl bg-emerald-600 px-5 py-3 font-semibold text-white">{b?'Analyzing and saving...':'Analyze & Save Result'}</button></form><div className="lg:col-span-2 rounded-2xl border bg-white p-6 shadow-sm">{!r?<div className="flex min-h-[250px] items-center justify-center text-sm text-slate-400">Saved AI result will appear here.</div>:<div><div className="flex justify-between border-b pb-4"><div><p className="text-xs uppercase text-slate-400">AI result</p><h2 className="text-xl font-bold">{r.decision}</h2></div><span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-semibold text-emerald-700">Saved</span></div><pre className="mt-5 max-h-[420px] overflow-auto rounded-xl bg-slate-950 p-4 text-xs text-white">{JSON.stringify(r,null,2)}</pre></div>}</div></div></div>}