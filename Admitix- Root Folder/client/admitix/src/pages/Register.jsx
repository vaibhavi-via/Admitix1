import {useState} from 'react';import{useNavigate,Link}from'react-router-dom';import{useAuth}from'../context/AuthContext';export default function Register(){const{register}=useAuth();const navigate=useNavigate();const[form,setForm]=useState({first_name:'',last_name:'',email:'',password:'',confirmPassword:'',institution_code:'',phone:''});const[error,setError]=useState('');const[busy,setBusy]=useState(false);const change=e=>setForm({...form,[e.target.name]:e.target.value});const submit=async e=>{e.preventDefault();setError('');if(!form.first_name||!form.email||!form.password||!form.confirmPassword||!form.institution_code)return setError('Please fill in all required fields.');if(form.password.length<8)return setError('Password must be at least 8 characters.');if(form.password!==form.confirmPassword)return setError('Passwords do not match.');setBusy(true);try{await register({first_name:form.first_name,last_name:form.last_name||null,email:form.email,password:form.password,phone:form.phone||null,institution_code:form.institution_code.toUpperCase()});navigate('/dashboard',{replace:true})}catch(e){setError(e.message||'Could not create account.')}finally{setBusy(false)}};return <><h1 className="text-xl font-semibold text-gray-900">Create your student account</h1><p className="mb-6 mt-1 text-sm text-gray-500">Use the institution code provided by your college.</p><form onSubmit={submit} className="space-y-4"><div className="grid grid-cols-2 gap-3"><div><label>First name</label><input name="first_name" value={form.first_name} onChange={change}/></div><div><label>Last name</label><input name="last_name" value={form.last_name} onChange={change}/></div></div><div><label>Institution code</label><input name="institution_code" value={form.institution_code} onChange={change} placeholder="e.g. ABCENG"/></div><div><label>Email</label><input name="email" type="email" value={form.email} onChange={change}/></div><div><label>Phone (optional)</label><input name="phone" value={form.phone} onChange={change}/></div><div><label>Password</label><input name="password" type="password" value={form.password} onChange={change}/></div><div><label>Confirm password</label><input name="confirmPassword" type="password" value={form.confirmPassword} onChange={change}/></div>{error&&<p className="rounded-lg bg-red-50 px-3 py-2 text-sm text-red-600">{error}</p>}<button disabled={busy} className="w-full rounded-xl bg-emerald-600 px-4 py-2.5 text-sm font-semibold text-white disabled:opacity-60">{busy?'Creating account...':'Create account'}</button></form><p className="mt-5 text-center text-sm text-gray-500">Already have an account? <Link to="/login" className="font-semibold text-emerald-600">Sign in</Link></p></>}
