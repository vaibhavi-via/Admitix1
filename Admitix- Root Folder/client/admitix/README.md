# Admitix Frontend

React + Vite frontend. Authentication is connected to the FastAPI backend; mock auth is disabled.

```bash
npm install
npm run dev
```

Set `VITE_API_BASE_URL=http://localhost:8000` in `.env`.
