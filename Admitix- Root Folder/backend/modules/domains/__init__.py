"""Domains module package.

A "domain" is the academic/professional field an institution belongs
to (Engineering, Medical, Law, Pharmacy, ...). Institutions are linked
to a domain via `institutions.domain_id` so the rest of the platform
can filter/report by domain without relying on free-text name matching.
"""
